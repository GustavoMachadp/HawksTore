import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Projeto-skate',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
